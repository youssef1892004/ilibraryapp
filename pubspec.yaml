# File path: pubspec.yaml
name: ilibrary_app
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  # --- ✅ تم التحديث لاستخدام النسخة المحسنة والمتوافقة ---
  better_player_enhanced: ^0.0.4
  cloudinary_flutter: ^1.3.0
  cloudinary_url_gen: ^1.0.0
  # الحزم الأخرى
  animated_text_kit: ^4.2.2
  graphql_flutter: ^5.2.0
  http: ^1.2.0
  provider: ^6.1.2
  shared_preferences: ^2.2.2
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/images/
    - assets/videos/
    - assets/audio/

